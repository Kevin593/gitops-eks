apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: git-clone
  namespace: tekton-pipelines
spec:
  params:
    - name: repo-url
      type: string
    - name: revision
      type: string
      default: master   # 👈 CAMBIO CLAVE
  workspaces:
    - name: output
  steps:
    - name: clone
      image: alpine/git
      script: |
        git clone -b $(params.revision) $(params.repo-url) $(workspaces.output.path)


